<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.javablacksmith.meeting.mapper.DepartmentMapper">
    <select id="getDepById" resultType="org.javablacksmith.meeting.model.Department">
        select * from department where departmentid = #{id};
    </select>
</mapper>